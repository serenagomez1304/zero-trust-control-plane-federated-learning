Ablation summary — 5 scenarios, 1 configurations
======================================================================

Scenarios where the BASELINE configuration *should* deny (expected_deny=True): 4

Per-configuration deny rate on the expected-deny subset:
  configuration   denies     rate
  BASELINE             4  100.0%

Marginal contribution per stage (scenarios denied at baseline but
allowed when this stage is removed):

Stage attribution at BASELINE (which stage caught each denied scenario):
  MICROSEG         4
